<p id="notice"><%= notice %></p>

<div id="app">
    <label for="read">カメラを起動してQRコードを読み取る</label>
    <input id="read" type="file" name="image" accept="image/*" capture>
</div>

<% if @events.empty? %>
  <%= link_to new_event_path do %>
    <div class="btn btn-primary">イベントを作成する</div>
  <% end %>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Mode</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @events.each do |event| %>
        <tr>
          <td><%= event.title %></td>
          <td><%= event.mode_i18n %></td>
          <td><%= link_to 'Show', event %></td>
          <td><%= link_to 'Edit', edit_event_path(event) %></td>
          <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  </br>
  <%= link_to 'イベントを作成する', new_event_path %>
<% end %>
